<p id="notice"><%= notice %></p>

<h1>Devices</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Brand</th>
      <th>Model</th>
      <th>Serial</th>
      <th>Last maintenance</th>
      <th>Area</th>
      <th>Device type</th>
      <th>Critical level</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @devices.each do |device| %>
      <tr>
        <td><%= device.name %></td>
        <td><%= device.brand %></td>
        <td><%= device.model %></td>
        <td><%= device.serial %></td>
        <td><%= device.last_maintenance %></td>
        <td><%= device.area_id %></td>
        <td><%= device.device_type_id %></td>
        <td><%= device.critical_level_id %></td>
        <td><%= link_to 'Show', device %></td>
        <td><%= link_to 'Edit', edit_device_path(device) %></td>
        <td><%= link_to 'Destroy', device, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Device', new_device_path %>
